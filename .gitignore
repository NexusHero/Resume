*.aux
*.out
*.synctex.gz
*.log
*.cls
node_modules/

# Build & tooling output
dist/
core/dist/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
# Generated Swagger UI site (built in CI, published to GitHub Pages — see docs:pages)
/site/

# Secrets / local env
.env
.env.*
!.env.example

# Generated résumé PDFs at repo root (the application archive is kept, see below)
/*.pdf

# Keep the application archive
!archive/bewerbungen/
!archive/bewerbungen/*.pdf

# Vite build output (recruiting kit)
design/myjob/ui_kits/recruiting/dist/

# Capacitor native platform projects (generated by `cap add` on a dev machine
# with the Android SDK / Xcode; not committed — see docs/native-app.md, ADR-0040)
/android/
/ios/
