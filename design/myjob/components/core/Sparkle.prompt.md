The AI-signature glyph — put it on everything the KI produces (match reasons, KI-Kurzprofil, magic buttons), never on deterministic UI.

```jsx
<span style={{ color: 'var(--accent-strong)' }}><Sparkle size={14} /></span>
```

Inherits `currentColor`. Pair with `--ai-grad`/`--accent-strong` per the AI-signature rule in readme.md.
